const insertArray = [5, 3, 14, 2, 1]

function insertionSorting(array) {
  for (let i = 0; i < array.length; i++) {
    for (j = i + 1; j < array.length; j++) {
        if (array[j] < array[i]) {
          
      }
    }
  }

  return array
}
