import React from "react"
import { useSelector } from "react-redux"

function Header() {
  return (
    <div>
      <div>
        <h2>Eccomerce Shop</h2>
      </div>
    </div>
  )
}

export default Header
